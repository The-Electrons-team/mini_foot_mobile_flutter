// Stub pour plateformes non-web (Android, iOS, Desktop, VM tests).
// Ne fait rien — les notifications navigateur ne sont pas disponibles.
void showBrowserNotification(String? title, String? body) {
  // no-op
}
